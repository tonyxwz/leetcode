"""
remember helper:
dijkstra is an extension of BFS, using gready strategy
that means, use BFS, but instead of using a queue, we use a min heap to
select the vertex with the smallest distance so far
"""

import heapq


def dijkstra(g, s):
    pass
