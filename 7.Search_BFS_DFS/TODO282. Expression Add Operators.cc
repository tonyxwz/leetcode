#include <string>
#include <vector>
#include <map>
#include <functional>
using namespace std;

class Solution {
  private:
    const map<char, strin
public:
    vector<string> addOperators(string num, int target) {
        
    }
};
